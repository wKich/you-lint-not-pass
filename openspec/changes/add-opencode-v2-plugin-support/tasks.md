# Tasks

## 1. Core patch support (src/enforce-write-policy.mjs)

- [x] 1.1 Add patch payload recognition and parsing — sniff `patchText`/patch markers into a `patch` tool name and parse `*** Begin Patch`/`*** End Patch` envelopes into per-file sections (`Add File`, `Update File` with optional `Move to`, `Delete File`); verify with new Bun tests asserting extracted paths and sections from a sample patch
- [x] 1.2 Enforce protected-config rules on patch sections (source or destination path protected: add, update, delete, move-away, move-onto) and verify with tests: deleting `.oxlintrc.json` via patch is denied, `Move to: biome.json` is denied, renaming a protected config away is denied, unrelated patch paths unaffected
- [x] 1.3 Enforce suppression-marker scan over `+`-prefixed added lines across all sections, denying the whole patch and naming every offending file; verify with tests: `+// eslint-disable-next-line` denied with file named, two offending files both named, context lines containing existing markers not flagged, clean patch allowed
- [x] 1.4 Implement fail-closed handling for unparseable patch payloads (identified as patch but not parseable into sections → deny with "could not be verified" reason); verify with a test asserting a malformed payload is denied rather than allowed
- [x] 1.5 Run the full existing suite plus new tests and verify `bun test`, `bun run typecheck`, and `bun run lint` all pass with no suppression comments added (project policy)

## 2. OpenCode V2 plugin port (opencode/you-lint-not-pass-plugin.ts)

- [x] 2.1 Rewrite the plugin as a V2 default export `Plugin.define({ id: 'you-lint-not-pass', setup(ctx) })` importing from `@opencode/plugin`, registering `ctx.tool.hook('execute.before', ...)` with `ctx.location.directory` as cwd, and throwing `new Error(reason)` on policy block; verify `bun run typecheck` passes
- [x] 2.2 Adapt V2 tool inputs — route only `write`, `edit`, and patch tools; map `event.input.path` to `file_path`; recognize both `patch` and `apply_patch` tool names; verify with tests using a fake `ctx` that captures the registered hook and asserts: violating write throws with policy reason, violating edit throws, non-edit tools pass through untouched, `apply_patch` spelling routed
- [x] 2.3 Update `.opencode/package.json` to depend on `@opencode/plugin` instead of `@opencode-ai/plugin`; verify dependencies install cleanly (`bun install`) and typecheck still passes

## 3. Documentation (README.md)

- [x] 3.1 Update Integration Guide Step 3 and the Template Files table: dependency pin becomes `@opencode/plugin`, re-export path unchanged, add an explicit note that the plugin targets OpenCode 2 only; verify by grepping the README that `@opencode-ai/plugin` no longer appears outside a migration note

## 4. Verification

- [x] 4.1 Probe whether `tool.execute.before` fires for tool calls nested inside Code Mode `execute` (attempt a nested `write` of a `// @ts-ignore` in a session with the plugin loaded) and record the finding in this task list; if hooks do not fire there, note it as a documented limitation and propose a follow-up permission-rule change rather than altering this change — **FINDING: the question is moot in the default configuration.** A nested `tools.write(...)` inside `execute` throws `Error: Unknown tool 'write'` at dispatch (file never created); `search()` from inside Code Mode confirms the catalog exposes only `browser`, `context7`, `opencode`, and `synthetic` namespaces — no filesystem write/edit/patch tools are reachable from `execute`, so no bypass exists and no follow-up change is needed. Residual limitation: a custom catalog exposing an MCP filesystem tool would not be covered by this plugin (host permissions still apply) — out of scope here.
- [x] 4.2 Run the official V2 plugin verification checklist in OpenCode 2: plugin ID appears in the active plugin list, inserting `// @ts-ignore` via the `write` tool is blocked with the policy reason, editing `.oxlintrc.json` is blocked, a compliant edit succeeds, and plugin reload/cleanup works; verify each observed behavior — **OBSERVED** (all probes via `opencode run --standalone --auto`, plugin discovered from `.opencode/plugins/`): (1) `opencode plugin list` shows `you-lint-not-pass` / local source ✓; (2) write of `// @ts-ignore` denied with the policy reason, file not created, no workaround attempted ✓; (3) edit of `.oxlintrc.json` denied with `Cannot modify ...`, file unchanged ✓; (4) compliant write and compliant edit both succeed with verified file contents ✓; (5) removal/cleanup: **not fully observable** — `opencode plugin list` serves stale state after the plugin file is removed (live-disk removal still listed; restored afterward). Structural note: `setup()` returns no cleanup function and owns no timers/sockets/subprocesses, so there is nothing plugin-side to dispose beyond the single hook registration, which the framework disposes on unload per the official plugin docs. Unit-level hook registration is covered by `tests/you-lint-not-pass-plugin.test.ts`.
- [x] 4.3 Final gate: `bun test`, `bun run typecheck`, and `bun run lint` all pass on the finished change
